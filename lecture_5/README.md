# Simple Book Collection API

This small FastAPI project implements CRUD for a book collection using SQLAlchemy and SQLite.

--- ----

## Run

1. Create virtual environment and install requirements:

```bash
python -m venv .venv
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
pip install -r requirements.txt
```

2. Run the app:

```bash
uvicorn main:app --reload
```

3. Open docs: http://127.0.0.1:8000/docs

--- ----

## Notes

- Code contains `--- ----` separators and `#` comments as requested.
